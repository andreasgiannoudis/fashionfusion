/**
 * This is the main app.js file that connects and imports all other JavaScript modules.
 * It imports the following modules:
 * - popup: Handles the popup functionality.
 * - blink: Manages elements with blinking effects.
 * - toggle-trigger: Controls elements that can be toggled.
 */
import "./popup";
import "./blink";
import "./toggle-trigger";